# Lean build output
.lake/
*.olean
*.ilean

# TeX build output, including the PDF — the .tex is the source of truth
tex/*.pdf
tex/*.aux
tex/*.log
tex/*.out
tex/*.toc
tex/*.synctex.gz
tex/*.fls
tex/*.fdb_latexmk

# blueprint build output, once increment 1 lands
blueprint/print/
blueprint/web/

# Palomar Comparator tool cache
.cache/

# private working notes, never committed
private/

# tooling scratch
.codegraph/
.cursor/
__pycache__/
*.pyc
.DS_Store
